<?xml version="1.0" encoding="UTF-8"?>
<!--
    ActiveMQ Classic Broker Configuration
    Docs: https://activemq.apache.org/xml-configuration

    KEY FIX: Do NOT import jetty.xml here. ActiveMQ 5.18.x manages the
    web console via its own internal jetty.xml in the conf directory.
    Mounting a custom jetty.xml breaks the webapps scanner that serves /admin.
-->
<beans
    xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://activemq.apache.org/schema/core
        http://activemq.apache.org/schema/core/activemq-core.xsd">

    <!-- ================================================================== -->
    <!--  BROKER CONFIGURATION                                                -->
    <!-- ================================================================== -->
    <broker xmlns="http://activemq.apache.org/schema/core"
            brokerName="localhost"
            dataDirectory="${activemq.data}"
            advisorySupport="false"
            useJmx="true"
            schedulerSupport="true">

        <!-- ============================================================== -->
        <!--  DESTINATIONS (Pre-defined Queues and Topics)                   -->
        <!-- ============================================================== -->
        <destinations>
            <queue physicalName="queue.orders"/>
            <queue physicalName="queue.notifications"/>
            <topic physicalName="topic.events"/>
            <topic physicalName="topic.alerts"/>
        </destinations>

        <!-- ============================================================== -->
        <!--  DESTINATION POLICIES                                           -->
        <!-- ============================================================== -->
        <destinationPolicy>
            <policyMap>
                <policyEntries>
                    <!-- Default policy for all queues -->
                    <policyEntry queue=">"
                                 producerFlowControl="true"
                                 memoryLimit="64mb"
                                 useDeadLetterQueue="true"
                                 expireMessagesPeriod="30000">
                        <deadLetterStrategy>
                            <individualDeadLetterStrategy
                                queuePrefix="DLQ."
                                useQueueForQueueMessages="true"
                                processExpired="true"
                                processNonPersistent="false"/>
                        </deadLetterStrategy>
                    </policyEntry>

                    <!-- Default policy for all topics -->
                    <policyEntry topic=">"
                                 producerFlowControl="true"
                                 memoryLimit="32mb">
                        <subscriptionRecoveryPolicy>
                            <timedSubscriptionRecoveryPolicy recoverDuration="300000"/>
                        </subscriptionRecoveryPolicy>
                    </policyEntry>
                </policyEntries>
            </policyMap>
        </destinationPolicy>

        <!-- ============================================================== -->
        <!--  MANAGEMENT CONTEXT                                             -->
        <!-- ============================================================== -->
        <managementContext>
            <managementContext createConnector="false"/>
        </managementContext>

        <!-- ============================================================== -->
        <!--  PERSISTENCE (KahaDB)                                           -->
        <!-- ============================================================== -->
        <persistenceAdapter>
            <kahaDB directory="${activemq.data}/kahadb"
                    journalMaxFileLength="32mb"
                    cleanupInterval="30000"
                    checkpointInterval="5000"
                    enableJournalDiskSyncs="true"
                    concurrentStoreAndDispatchQueues="true"/>
        </persistenceAdapter>

        <!-- ============================================================== -->
        <!--  SYSTEM USAGE (Memory / Storage Limits)                         -->
        <!-- ============================================================== -->
        <systemUsage>
            <systemUsage sendFailIfNoSpace="false">
                <memoryUsage>
                    <memoryUsage percentOfJvmHeap="70"/>
                </memoryUsage>
                <storeUsage>
                    <storeUsage limit="5 gb"/>
                </storeUsage>
                <tempUsage>
                    <tempUsage limit="500 mb"/>
                </tempUsage>
            </systemUsage>
        </systemUsage>

        <!-- ============================================================== -->
        <!--  TRANSPORT CONNECTORS                                           -->
        <!-- ============================================================== -->
        <transportConnectors>
            <!-- OpenWire (Standard JMS) -->
            <transportConnector name="openwire"
                uri="tcp://0.0.0.0:61616?maximumConnections=1000&amp;wireFormat.maxFrameSize=104857600&amp;wireFormat.maxInactivityDuration=0"/>

            <!-- AMQP -->
            <transportConnector name="amqp"
                uri="amqp://0.0.0.0:5672?maximumConnections=1000&amp;wireFormat.maxFrameSize=104857600"/>

            <!-- STOMP -->
            <transportConnector name="stomp"
                uri="stomp://0.0.0.0:61613?maximumConnections=1000&amp;wireFormat.maxFrameSize=104857600"/>

            <!-- STOMP over WebSocket -->
            <transportConnector name="stomp+ws"
                uri="stomp+ws://0.0.0.0:61614?maximumConnections=500"/>

            <!-- MQTT -->
            <transportConnector name="mqtt"
                uri="mqtt://0.0.0.0:1883?maximumConnections=1000&amp;wireFormat.maxFrameSize=104857600"/>
        </transportConnectors>

        <!-- ============================================================== -->
        <!--  PLUGINS                                                         -->
        <!-- ============================================================== -->
        <plugins>
            <!-- Authentication -->
            <simpleAuthenticationPlugin anonymousAccessAllowed="false">
                <users>
                    <authenticationUser username="admin"     password="admin"        groups="admins,publishers,consumers"/>
                    <authenticationUser username="publisher" password="publisher123"  groups="publishers"/>
                    <authenticationUser username="consumer"  password="consumer123"   groups="consumers"/>
                </users>
            </simpleAuthenticationPlugin>

            <!-- Authorization -->
            <authorizationPlugin>
                <map>
                    <authorizationMap>
                        <authorizationEntries>
                            <authorizationEntry queue=">"  read="admins"             write="admins"             admin="admins"/>
                            <authorizationEntry topic=">"  read="admins"             write="admins"             admin="admins"/>
                            <authorizationEntry queue="queue.>" read="admins,consumers" write="admins,publishers"  admin="admins"/>
                            <authorizationEntry topic="topic.>" read="admins,consumers" write="admins,publishers"  admin="admins"/>
                            <!-- Advisory topics REQUIRED for web console to work -->
                            <authorizationEntry topic="ActiveMQ.Advisory.>"
                                read="admins,publishers,consumers"
                                write="admins,publishers,consumers"
                                admin="admins"/>
                        </authorizationEntries>
                    </authorizationMap>
                </map>
            </authorizationPlugin>

            <!-- Stats plugin (powers the web console metrics) -->
            <statisticsBrokerPlugin/>

            <!-- Timestamps on messages -->
            <timeStampingBrokerPlugin zeroAdditionalTime="true" futureOnly="false"/>
        </plugins>

    </broker>

    <!--
        Do NOT add <import resource="jetty.xml"/> here.
        ActiveMQ 5.18.x auto-loads jetty.xml from conf/ internally.
        Importing it again causes a double-start of Jetty and breaks /admin.
    -->

</beans>
